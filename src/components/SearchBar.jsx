import React from 'react';

const SearchBar = () => {
    return (
        <div>
            <label htmlFor="">{}</label>
            <input type="text" placeholder='Search' />
        </div>
    );
}

export default SearchBar;
